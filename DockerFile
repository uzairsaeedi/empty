# Use the official Nginx image from Docker Hub
FROM nginx:alpine

# Copy your static website files (HTML, CSS, etc.) into the container's Nginx directory
# Replace ./dist with the folder where your HTML/CSS files are located (if different)
COPY ./ /usr/share/nginx/html

# Expose port 80 so we can access the website through this port
EXPOSE 80

# Start Nginx when the container runs
CMD ["nginx", "-g", "daemon off;"]
